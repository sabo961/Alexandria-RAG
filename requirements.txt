# Alexandria RAG System - Python Dependencies
# Python 3.14+ required

# Vector Database
qdrant-client>=1.7.1

# Embedding & Semantic Analysis
sentence-transformers>=2.3.1  # all-MiniLM-L6-v2 (384-dim embeddings)
torch>=2.0.0  # Required by sentence-transformers
numpy>=1.24.0  # Universal chunking semantic analysis
scikit-learn>=1.3.0  # Cosine similarity for semantic chunking

# Book Parsing - EPUB
EbookLib==0.18

# Book Parsing - PDF
PyMuPDF>=1.24.0

# HTML Parsing (for EPUB content extraction)
beautifulsoup4==4.12.2
lxml==4.9.3

# Optional: MOBI support (requires additional setup)
# mobi==0.3.3

# HTTP Client
requests>=2.31.0  # OpenRouter API calls

# Utilities
tqdm==4.66.1  # Progress bars (disabled globally via TQDM_DISABLE=1)

# GUI
streamlit>=1.29.0  # Web interface (binds to 0.0.0.0:8501)

# Development (Recommended - Not Yet Implemented)
pytest==7.4.3  # Testing framework
pytest-cov==4.1.0  # Coverage reports
black==23.12.1  # Code formatter
flake8==7.0.0  # Linter
