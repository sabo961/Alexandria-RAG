# Alexandria Configuration
# Copy this file to .env and edit with your values:
#   cp .env.example .env

# Qdrant Vector Database
QDRANT_HOST=192.168.0.151
QDRANT_PORT=6333
QDRANT_COLLECTION=alexandria

# Calibre Library (path to your Calibre library folder)
# On Windows with NAS, use forward slashes: //Server/share/path
CALIBRE_LIBRARY_PATH=//YourNAS/calibre/library
CALIBRE_WEB_URL=https://your-calibre-web-instance.example.com

# CWA Auto-Ingest Folder (Calibre-Web Automated ingest directory)
# On Windows with NAS, use forward slashes: //Server/share/path
CWA_INGEST_PATH=//YourNAS/calibre-web/ingest

# Local File Ingestion (default browse directory)
LOCAL_INGEST_PATH=C:\Users\YourName\Downloads

# Embedding Model Configuration
# Available: minilm (384-dim), bge-large (1024-dim), bge-m3 (1024-dim, multilingual)
DEFAULT_EMBEDDING_MODEL=bge-m3
EMBEDDING_DEVICE=auto

# OpenRouter API (optional - only needed for CLI --answer testing)
# Get your key at: https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-v1-your-key-here
